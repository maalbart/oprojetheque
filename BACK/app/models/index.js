
/* file that collects all require models and exports them */
const Project = require('./project');
const Promo = require('./promo');
const Student = require('./student');

module.exports = {
    Project,
    Promo,
    Student
}